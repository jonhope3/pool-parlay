{
  "name": "pool-parlay",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "snapshot": "node scripts/snapshot-nfl.mjs",
    "build": "npm run snapshot && tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "peerjs": "^1.5.5"
  },
  "devDependencies": {
    "typescript": "~5.9.2",
    "vite": "^7.1.5"
  }
}
